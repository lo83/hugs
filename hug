local ScreenGui = Instance.new("ScreenGui")
local ToggleButton = Instance.new("TextButton")
local Frame = Instance.new("Frame")
local TargetInput = Instance.new("TextBox")
local OnButton = Instance.new("TextButton")
local OffButton = Instance.new("TextButton")

ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

ToggleButton.Parent = ScreenGui
ToggleButton.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
ToggleButton.Position = UDim2.new(0, 10, 0.4, 0)
ToggleButton.Size = UDim2.new(0, 80, 0, 40)
ToggleButton.Text = "Menu"

Frame.Parent = ScreenGui
Frame.Visible = false
Frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Frame.Position = UDim2.new(0.4, 0, 0.3, 0)
Frame.Size = UDim2.new(0, 180, 0, 150)
Frame.Active = true
Frame.Draggable = true

ToggleButton.MouseButton1Click:Connect(function()
    Frame.Visible = not Frame.Visible
end)

TargetInput.Parent = Frame
TargetInput.Size = UDim2.new(1, 0, 0.3, 0)
TargetInput.PlaceholderText = "2 Pit..."

OnButton.Parent = Frame
OnButton.Position = UDim2.new(0, 0, 0.35, 0)
OnButton.Size = UDim2.new(1, 0, 0.3, 0)
OnButton.Text = "ON (Kiss Mode)"
OnButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50) -- ڕەنگێ سۆر

OffButton.Parent = Frame
OffButton.Position = UDim2.new(0, 0, 0.7, 0)
OffButton.Size = UDim2.new(1, 0, 0.3, 0)
OffButton.Text = "STOP"
OffButton.BackgroundColor3 = Color3.fromRGB(150, 0, 0)

local active = false

OnButton.MouseButton1Click:Connect(function()
    local targetName = TargetInput.Text:lower()
    if targetName == "" then return end
    active = true
    
    while active do
        task.wait(0.01)
        for _, v in pairs(game.Players:GetPlayers()) do
            if v ~= game.Players.LocalPlayer and v.Name:lower():sub(1, #targetName) == targetName then
                local char = game.Players.LocalPlayer.Character
                local tChar = v.Character
                if char and tChar and tChar:FindFirstChild("HumanoidRootPart") then
                    -- ئەڤە تە ڕێک دکەتە بەرامبەر وی (Kiss)
                    -- لێرە مە Angles گوهۆڕی بۆ 180 پلە دا کو ڕوویێ تە ل وی بیت
                    char.HumanoidRootPart.CFrame = tChar.HumanoidRootPart.CFrame * CFrame.new(0, 0, -1.1) * CFrame.Angles(0, math.rad(180), 0)
                end
            end
        end
    end
end)

OffButton.MouseButton1Click:Connect(function()
    active = false
end)
